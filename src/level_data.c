#include "level_data.h"

/*
 * Level data is intentionally header-defined so the generated tables remain
 * easy to inspect and regenerate.  This translation unit gives build systems
 * a stable source file to compile alongside the game.
 */
